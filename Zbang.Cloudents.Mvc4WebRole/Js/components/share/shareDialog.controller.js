var app;
(function (app) {
    "use strict";
    var ShareDialog = (function () {
        function ShareDialog() {
        }
        return ShareDialog;
    }());
    angular.module("app").controller("ShareDialog", ShareDialog);
})(app || (app = {}));
