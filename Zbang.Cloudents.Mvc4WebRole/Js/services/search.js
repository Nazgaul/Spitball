define('search', ['app'], function (app) {
    var Search = '/Search';
    app.factory('Search',
        ['$http',
         '$q',

        function ($http, $q) {
            return {               
            };
        }
        ]);
});