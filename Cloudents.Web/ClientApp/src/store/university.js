
const state = {
    selectedClasses: [],
};

const getters = {
    getSelectedClasses: state => state.selectedClasses,
};

const mutations = {
};

const actions = {
};

export default {
    state,
    mutations,
    getters,
    actions
};